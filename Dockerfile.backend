FROM node:20-alpine

WORKDIR /app

# Install deps and wget for healthcheck
COPY node/package*.json ./
RUN apk add --no-cache wget && npm install

# Copy source
COPY node/ .

ENV NODE_ENV=production
ENV PORT=3000

EXPOSE 3000

# Start the server directly with node
CMD ["node", "server.js"]
